# Project status

## Stato corrente

Legacy-active / stable.

Il progetto non viene lasciato in sospeso tra due architetture:

- la pipeline reale resta notebook-driven
- il valore pubblico del progetto vive nelle Discussions e nei notebook di risposta
- il repo viene chiuso documentando bene questo stato

## Perimetro confermato

- `saldi storici`
- `spese per missione / programma / macroaggregato`

Fuori perimetro:

- nuova linea `entrate` come dataset separato
- migrazione completa al `toolkit` in questo repo

## Stato delle Discussions principali

| Discussion | Tema | Stato repo |
|---|---|---|
| `#9` | Deficit strutturale | risposta pubblicata, notebook importato |
| `#16` | Ricorso al mercato | notebook importato e verificato |
| `#14` | Avanzo primario | materiale da riallineare nel repo |
| `#12` | Entrate tributarie | materiale da riallineare nel repo |
| `#10` | Spese correnti vs investimenti | notebook importato e verificato |
| `#17` | Interessi sul debito | notebook importato e verificato |

## Cosa manca per chiudere bene

1. collegare anche `#12` e `#14` a un notebook o commento dati dentro il repo
2. riallineare README / stato quando una Discussion cambia stato reale
3. lasciare esplicito cosa resta fuori dal progetto

Nota:

- `#12` e `#14` possono restare aperte in modo intenzionale come backlog contributivo leggero
- questo non riapre il perimetro del progetto, ma lascia due ingressi utili per follow-up futuri

## Follow-up possibile

Se in futuro nascerà una versione `toolkit-native`, dovrà essere trattata come filone separato e non come retrofit di questo repo legacy.
